<% provide :title, "Edit entry" %>
<% provide :site_section, "1" %>

<div class="content">
	<h1>Edit entry</h1>
</div>

<div class="content">

      <%= form_for(@creation, html: {class: "form-horizontal"}) do |f| %>
  	  <%= render 'shared/error_messages', object: f.object %>

          <div class="form-group">
            <%= f.label :name, class: "col-sm-2 control-label" %>
            <div class="col-sm-10">
              <%= f.text_field :name, class: "form-control" %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :url, class: "col-sm-2 control-label" %>
            <div class="col-sm-10">
              <%= f.text_field :url, class: "form-control" %>
				<small>Usually you should put the raw files in the public_html folder under the archives/[medium] folder, though obviously you can host them elsewhere on the Internet at large if you want. There'll be an upload facility implemented here sometime around the same time as I ensure the website can also solve the Halting Problem and make a decent cuppa. Maybe then I will have a friend *sob*</small>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :medium, class: "col-sm-2 control-label" %>
            <div class="col-sm-10">
		        <%= f.collection_select :medium, Creation::MEDIA, :to_s, :humanize, class: "form-control" %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :created_at, class: "col-sm-2 control-label" %>
            <div class="col-sm-10">
				<%= f.date_select :created_at, start_year: 1970, end_year: Date.today.year, class: "form-control" %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :description, class: "col-sm-2 control-label" %>
            <div class="col-sm-10">
              <%= f.text_field :description, class: "form-control" %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :keywords, class: "col-sm-2 control-label" %>
            <div class="col-sm-10">
              <%= f.text_field :keywords, class: "form-control" %>
				<small>Enter some keywords so users can search for this item. Doesn't need to contain commas. e.g. "chainwriting Tommy coffee"</small>
            </div>
          </div>

          <div class="actions form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <%= f.submit "Save", class: "btn btn-large btn-primary" %>
            </div>
          </div>

      <% end %>

</div>

<div class="content">
	<%= render 'shared/back_button', path: creative_path %>
</div>
