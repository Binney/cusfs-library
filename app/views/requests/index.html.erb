<% provide(:title, "Requests") %>
<% provide :site_section, "0" %>
<h1>Pending requests</h1>

<% if @requests.any? %>
	<%= will_paginate %>

	<ul class="requests">
		<%= render @requests %>
	</ul>

	<%= will_paginate %>
<% else %>
Nothing found.
<% end %>