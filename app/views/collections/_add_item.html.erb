<%= form_for(collection.exhibits.build(item_id: item_id)) do |f| %>
  <%= f.hidden_field :item_id %>
  <%= f.hidden_field :collection_id, value: collection.id %>
  <%= f.submit collection.name %>
<% end %>