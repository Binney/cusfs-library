<% if requests.any? %>
  <ul class="requests">
  <%= render requests %>
  </ul>
<% else %>
  None found.
  <% if signed_in? %>
  	<%= link_to "Request something!", new_request_path %>
  <% end %>
<% end %>