<% provide(:title, 'All users') %>
<% provide :site_section, '3' %>

<div class="content">
	<h1>All users</h1>
</div>

<div class="content content-mini">
	<%= will_paginate %>
</div>

<div class="content">
	<ul class="users">
	  <%= render @users %>
	</ul>
</div>

<div class="content content-mini">
	<%= will_paginate %>
</div>