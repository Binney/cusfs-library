<% provide(:title, "#{@user.name}'s reservations") %>
<% provide :site_section, '0' %>

<div class="content">
	<h1><%= @user.name %>'s reservations</h1>
</div>

<div class="content">
	<%= render 'reservations', reservations: @reservations %>
</div>

<div class="content">
	<%= link_to "Back to profile", @user %>
</div>