<%= @item.editions.length-@item.withdrawals.length %> of <%= @item.editions.length %> available ¤ 
<% if signed_in? && current_user.has_withdrawn?(@item) %>
  You've currently got this book on loan.
  <%= render 'items/unwithdraw' %>
<% end %>

<% if @item.is_available? %>
  <%= render 'items/withdraw_opts' %>
<% else %>
  This item's currently on loan.
<% end %>
