<% provide(:title, "Browse #{@title.downcase}") %>
<h1>Browse <%= @title.downcase %></h1>

<%= render 'search' %>

<% if @items.any? %>
	<%= will_paginate %>

	<ul class="items">
		<%= render @items %>
	</ul>

	<%= will_paginate %>
<% else %>
No items found.
<% end %>